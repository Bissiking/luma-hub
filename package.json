{
  "name": "luma-hub",
  "version": "0.1.0-dev.3",
  "description": "Portail principal LUMA pour mhemery.fr, connecté à Kyros.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/kyros.js && node --check src/session.js && node --check src/public/app.js && node --check src/public/enterprise-context.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
